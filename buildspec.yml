version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      -echo Echo install 

  pre_build:
    commands:
      -echo Pre Build

  build:
   commands:
    -echo build

  post_build:
    commands:
      -echo post build
    